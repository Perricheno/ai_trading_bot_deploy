services:
  - type: worker
    name: ai-trading-bot
    env: python
    buildCommand: pip install -r requirements.txt && python train.py
    startCommand: python main.py
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
